#include <iostream>
using namespace std;
int main() {
    int minutes, fps;
    cout << "Number of Minutes: ";
    cin >> minutes;
    cout << "Frames per Second: ";
    cin >> fps;
    cout << "Total Number of Frames: " << minutes * 60 * fps << endl;
    return 0;
}